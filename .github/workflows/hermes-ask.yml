name: Hermes Ask Router Agent

on:
  repository_dispatch:
    types: [hermes_request]
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      question:
        description: "Question or Prompt for Hermes Agent"
        required: true
        type: string
      model:
        description: "Optional custom model override (e.g. qwen/qwen3.6-27b, codestral-latest:free, gemini-3.7-flash-free)"
        required: false
        type: string
      user_id:
        description: "User Identifier for Isolated Memory"
        required: false
        type: string
      target_layer:
        description: "Direct layer trigger override (claude, chatgpt, gemini, background, memory, direct, master)"
        required: false
        type: string

permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: write

concurrency:
  group: hermes-ask-sync-${{ github.ref }}
  cancel-in-progress: false

jobs:
  route-and-dispatch:
    if: ${{ (github.event_name == 'repository_dispatch') || (github.event_name == 'workflow_dispatch') || (contains(github.event.comment.body, '/ask') && !github.event.comment.user.bot) }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Resolve Target Layer Workflow & Model Provider
        id: router
        env:
          RAW_PROMPT: ${{ github.event.client_payload.question || inputs.question || github.event.comment.body }}
          MANUAL_LAYER: ${{ inputs.target_layer }}
          INPUT_MODEL: ${{ github.event.client_payload.model || inputs.model }}
        run: |
          PROMPT_LOWER=$(echo "$RAW_PROMPT" | tr '[:upper:]' '[:lower:]')
          
          # Extract --model override if present in issue comments
          EXTRACTED_MODEL=""
          if echo "$RAW_PROMPT" | grep -qE '--model[ =]'; then
            EXTRACTED_MODEL=$(echo "$RAW_PROMPT" | grep -oE '--model[ =][a-zA-Z0-9.\/:-]+' | awk '{print $2}' || true)
          fi

          EFFECTIVE_MODEL="${INPUT_MODEL:-$EXTRACTED_MODEL}"
          echo "EFFECTIVE_MODEL=$EFFECTIVE_MODEL" >> $GITHUB_ENV

          TARGET="claude-os.yml"
          if [ -n "$MANUAL_LAYER" ]; then
            case "$MANUAL_LAYER" in
              claude) TARGET="claude-os.yml" ;;
              chatgpt) TARGET="chatgpt-research.yml" ;;
              gemini) TARGET="gemini-workspace.yml" ;;
              background) TARGET="background-orchestrator.yml" ;;
              memory) TARGET="memory-sync.yml" ;;
              direct|master) TARGET="hermes-master.yml" ;;
            esac
          else
            if echo "$PROMPT_LOWER" | grep -qE "code|script|build|fix|refactor|pull request|pr|issue|deploy|git|commit"; then
              TARGET="hermes-master.yml"
            elif echo "$PROMPT_LOWER" | grep -qE "research|scrape|excel|xlsx|pdf|search|report"; then
              TARGET="chatgpt-research.yml"
            elif echo "$PROMPT_LOWER" | grep -qE "gmail|docs|calendar|drive|workspace|audio|video"; then
              TARGET="gemini-workspace.yml"
            elif echo "$PROMPT_LOWER" | grep -qE "cron|schedule|loop|retry|audit|background"; then
              TARGET="background-orchestrator.yml"
            elif echo "$PROMPT_LOWER" | grep -qE "memory|context|user state|vector|store"; then
              TARGET="memory-sync.yml"
            else
              TARGET="claude-os.yml"
            fi
          fi

          echo "DISPATCH_TARGET=$TARGET" >> $GITHUB_ENV
          echo "RAW_PROMPT_VAL=$RAW_PROMPT" >> $GITHUB_ENV
          echo "Target selected: $TARGET (Model: ${EFFECTIVE_MODEL:-Default})"

      - name: Dispatch Worker Sub-Workflow
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.HERMES_PAT || secrets.GITHUB_TOKEN }}
          script: |
            const targetWorkflow = process.env.DISPATCH_TARGET;
            const question = process.env.RAW_PROMPT_VAL || "Default query executed by router";
            const userId = "${{ github.event.client_payload.user_id || inputs.user_id || (github.event.comment && github.event.comment.user.login) || 'public_user' }}";
            const modelOverride = process.env.EFFECTIVE_MODEL || "";
            const issueNumber = context.issue ? context.issue.number : null;

            console.log(`Dispatching target: ${targetWorkflow} for user ${userId} with model: ${modelOverride || 'default'}`);

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: targetWorkflow,
              ref: context.ref || 'main',
              inputs: {
                question: question,
                command: question,
                query: question,
                user_id: userId,
                model: modelOverride,
                issue_number: issueNumber ? String(issueNumber) : "",
                action_type: "auto_routed",
                payload: JSON.stringify({ question, userId, model: modelOverride }),
                job_id: `job-${Date.now()}`,
                memory_payload: question
              }
            });
            
